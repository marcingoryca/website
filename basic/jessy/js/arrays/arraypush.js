// Array.push()
// Adds item to the end of the array

(function () {
    "use strict";

    let colors = [];

    // Print what we have here
    console.log(colors);

    // Let's add some elements
    colors.push("red", "green", "blue", "black");

    // Print existing array
    console.log(colors);
})(); 