( function () {
    "use strict";
    let x = 20;
    x = -x;     //unary operator, it has single operand
    console.log(x);

    let y = 17;
    let z = 9;
    let a = y / z;  //binary operator, it has two operands
    console.log(a);
})();